VITE_CODESPACE_NAME=

# If you run the frontend in a Codespace, set VITE_CODESPACE_NAME to your Codespace
# name so the frontend can target the preview backend URL:
# VITE_CODESPACE_NAME=my-codespace-name
